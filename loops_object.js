const myObject = {
  js: "javascript",
  cpp: "c++",
  rb: "rubi",
  py: "python",
};
for (const key in myObject) {
  //   console.log(myObject[key]);
  //   console.log(`${key} shortcut for  ${myObject[key]}`);
}

const programing = ["java", "c++", "python", "rubi"];
for (const key in programing) {
  console.log(programing[key]);
}

